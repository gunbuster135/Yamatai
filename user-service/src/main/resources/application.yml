

jwt:
  secret: ThisIsASecret
spring:
  data:
    mongodb:
      uri: mongodb://localhost/einharjar



server:
  port: 8080
eureka:
  instance:
    hostname: user-service
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8080/eureka/
